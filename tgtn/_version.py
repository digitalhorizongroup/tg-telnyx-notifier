"""Версия дистрибутива — единственное место, где записано её значение.

setuptools читает отсюда литерал через ``[tool.setuptools.dynamic] version =
{attr = "tgtn._version.__version__"}``. Модуль отдельный и без
импортов: по такому файлу setuptools берёт значение статически, разбором
синтаксического дерева, и пакет при сборке не импортируется. Строка в
``__init__`` привела бы к импорту ``aiogram`` и ``fastapi`` в момент сборки.

Формат — PEP 440, иначе сборка падает на проверке метаданных.
"""

__version__ = "0.3.0"
